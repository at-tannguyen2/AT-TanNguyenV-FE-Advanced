import { ToggeFavouriteDirective } from './togge-favourite.directive';

describe('ToggeFavouriteDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggeFavouriteDirective();
    expect(directive).toBeTruthy();
  });
});
