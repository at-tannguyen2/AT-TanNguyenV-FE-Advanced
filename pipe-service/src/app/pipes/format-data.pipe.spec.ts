import { FomartDataPipe } from './format-data.pipe';

describe('FomartDataPipe', () => {
  it('create an instance', () => {
    const pipe = new FomartDataPipe();
    expect(pipe).toBeTruthy();
  });
});
